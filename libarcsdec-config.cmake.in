@PACKAGE_INIT@

## TODO Use find_dependency to search for the libraries libarcsdec depends on
## This information should be already captured in the imported targets. (?)

include (${CMAKE_CURRENT_LIST_DIR}/libarcsdec-targets.cmake )
